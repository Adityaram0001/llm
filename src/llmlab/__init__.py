"""LLM-Lab: build and study a ~100M-parameter LLM on Apple Silicon.

Subpackages are created phase by phase (see docs/ROADMAP.md):
utils (P0), data (P1), tokenizer (P2), model (P3), train (P4), eval (P6).
"""

__version__ = "0.1.0"
