# Learnings index

One line per note, newest first. Notes are written at the end of discussion sessions
(see CLAUDE.md "Discussion sessions") — dense, revision-friendly, with the project's real
numbers. Not chat transcripts.

- [2026-07-19 — Phase 8 Part C deep dive (DPO from scratch): the RLHF→DPO derivation connected to logistic regression (Bradley-Terry substitution, β as regularization strength), reward_margin decoded into nats/implied odds ratio (margin 8.97 → ~10^39 odds by step 75), the length confound explained mechanistically + reconstructed arithmetically (+623.7 nats ≈ 124.9 extra tokens × ~5 nats/token) AND refined with a NEW per-failure-mode reward breakdown not run during training (wrong_fact 87.2%/1.69 margin vs off_format/verbose ~100%/5.6-20.1 margin — real content learning happened, but the gradient was dominated by the two length-correlated failure modes, explaining the answer-length collapse mechanistically instead of just noting it), three real literature citations tying the finding to published DPO/RLHF length-bias and overoptimization-scaling-law research (Singhal et al. '23, SimPO, Rafailov et al. '24), the honest MPS-slowdown investigation (what was ruled out vs what's still open), and the SFT→LoRA→DPO throughline (protocol vs cheap-protocol vs contrastive-preference, and why only the contrastive one opens the door to shortcut learning)](20260719_phase8-dpo-deep-dive.md)

- [2026-07-19 — Phase 8 Parts A & B deep dive (SFT + LoRA from scratch): perplexity = exp(cross-entropy) as an uncertainty ruler with worked nats/bits math (base ppl 34.9 out of 16k vocab), catastrophic forgetting defined + how the frozen-probe measures it (the "+27%" decoded as 44.4/34.9−1 = pretrain-ppl drift), the assistant-only loss mask worked through on a real token sequence + why it's exact-by-construction (dodges RW-6), full-FT vs the three LoRA configs technically and in our numbers (where params live: 1/3 attn vs 2/3 ffn → placement beats rank; best LoRA 3.777 beat full FT 3.828), LoRA math from scratch (W+(α/r)·BA, why B=0 + gradient flow, the rank↔alpha decoupling + our fixed-α confound, why the memory win is 13–53× optimizer state and scale-invariant to 7B+), the forgetting-reversibility nuance (frozen base is bit-identical, pop the adapter → ppl 34.9 back), and the two dtype/device bugs the fp32/CPU tests missed (D-051/D-052)](20260719_phase8-sft-lora-deep-dive.md)
- [2026-07-18 — Phase 7 data factory retrospective: the backend-agnostic pipeline (why `run` is the automated twin of the manual paste loop), the multi-backend decision and the grounded-vs-closed-book axis that justifies a small local model, five real bugs (grounding gate keyed off the wrong field, Gemma's U+2581 meta-space breaking 100% of JSON parses, mlx-lm silently upgrading transformers 4→5, the GCIDE numeral-seed problem, broken Homebrew whose Python the venv depends on), a live throughput benchmark (Gemma E4B ~28 tok/s local vs DeepSeek ~139 tok/s effective, with 3k-pair time/cost math), and a per-backend batch-sizing analysis showing API cost is output-token-dominated and batch-invariant while Gemma is bound by an 8k cap + Ollama's silent num_ctx truncation (D-048/D-049/RW-7)](20260718_phase7-data-factory.md)
- [2026-07-17 — Phase 6 eval suite deep dive: the shared log-likelihood scoring primitive + why length-normalization matters (worked example), the prompt/continuation BPE-boundary trick, a real 100%-reproducing content-corruption bug found in the definition-completion probe (full evidence + fix path, D-047/RW-6), perplexity-vs-bits-per-byte reconciled with real numbers, two different perplexity estimators now in the codebase, a full statistical-significance pass showing several "signals" aren't distinguishable from chance at the sample sizes used, a frequency-bias case study on an 8/8 proverb result, calibration/ECE explained via proper scoring rules, and the complete "what's covered vs not covered" inventory](20260717_phase6-eval-suite-deep-dive.md)
- [2026-07-16 — Wave G deep dive: RW-4's domain corpus curation (why auto-categorized book lists are noisy in a specific, predictable way), the mixing-weight sampling mechanism + budget-shrink arithmetic, a two-sided domain-mix result computed live in this session (domain val_loss improves in near-exact proportion to general val_loss's cost), why the train/val gap is the earlier overfitting signal than val level, and the mini scaling law's headline finding that bigger models overfit a small repeated pool FASTER (with the full best-vs-final table and the Muennighoff-ceiling tie-in)](20260716_wave-g-data-scaling.md)
- [2026-07-16 — Wave F deep dive: DeepSeekMoE's fine-grained-expert math worked in full (why expert_hidden=171, active-vs-total param arithmetic), the active-vs-total-capacity throughline across Waves C/E/F, aux_loss-vs-bias_free balancing explained as proportional vs bounded-step control (with the full per-step std/mean trajectory), the MTP index arithmetic + why it's train-time-only, the throughput cost (~2.18x slower) the original notes.md missed, and a full postmortem of the val_loss measurement bug caught before any verdict was written](20260716_wave-f-deepseek-specials.md)
- [2026-07-13 — Wave E efficiency & memory deep dive: bf16's "only matmuls get cast, master weights stay fp32" mechanism, gradient checkpointing's ~1.72x memory ratio derived + the surprising linear-not-quadratic seq_len scaling (flash attention), the launch-overhead math behind micro-batch/accum's >2x wall-clock spread despite identical loss, torch.compile's graph-capture cost visible in the raw log + the checkpoint key-naming fix, why a param-matched weight-tying rerun is harder than it sounds, and the free-wins stacking estimate for phase 9](20260713_wave-e-efficiency-memory.md)
- [2026-07-13 — Wave C attention variants deep dive: the KV-cache-as-inference-bottleneck framing with worked scale math, MHA/GQA/MQA one-knob spectrum, MLA's full decomposition + the nope/rope decoupling explained, why quality is flat so cache decides, the honest "cache ≠ speed at 10M params" finding + weight-absorption note, the params≠cache paradox, and why the wave had to run at n_heads=4](20260713_wave-c-attention-variants.md)
- [2026-07-13 — Wave D deep dive: Muon's Newton-Schulz orthogonalization + why its edge narrows but never closes, the WSD>constant>cosine schedule hierarchy explained, the WSD multi-budget checkpoint-fork demo, why grad-clip-off didn't spike, Lion/batch-size confounds flagged honestly, and whether any of it breaks re-running older waves](20260713_wave-d-optimizers-schedules.md)
- [2026-07-13 — Wave B positional encodings deep dive: the three encoding families + param consequences, the length-extrapolation probe explained as method (ALiBi improves to 2048, RoPE degrades gracefully, NoPE collapses, learned/sinusoidal physically can't run), the sinusoidal surprise-worst + honest why, RW-5's narrow forward() fix, and the phase-9 long-context/ALiBi-vs-RoPE decision it feeds](20260713_wave-b-positional-encodings.md)
- [2026-07-13 — Wave A norms & activations deep dive: RMSNorm≈LayerNorm (kept for cost, with the exact bias-param arithmetic), pre-norm≫post-norm (stagnation-not-divergence + the residual-init connection), SwiGLU>GELU (the 8·d² param-match algebra + GELU-bias asterisk), QK-norm the surprise win (why the widening gap proves it's real), and the noise floor earning its keep on day one](20260713_wave-a-norms-activations.md)
- [2026-07-12 — Sequence length vs. token count vs. model size: what each buys you, minimum config per learning goal mapped to phase 5's waves, why the capstone's chat-context need is a separate decision](20260712_model-config-strategy.md)
- [2026-07-12 — gpuhub RTX 4080 capacity: measured throughput/memory sweeps across tiers & seq_len, why tok/s regresses past micro_batch=32, cost estimates for S/M/L runs on the $0.25/hr tier](20260712_gpuhub-rtx4080-capacity.md)
- [2026-07-12 — R2 bucket file walkthrough: tokenizer artifacts vs tokenized data, docstarts/boundary-crossing default, Chinchilla margin math behind FineWeb-Edu](20260712_r2-data-files-walkthrough.md)
- [2026-07-11 — GPU choice ($/FLOP not VRAM), batch calibration vs dynamic adjustment, 32k-vocab v2 parking, domain data mixing math](20260711_gpu-vocab-datamix.md)
- [2026-07-11 — Parameter allocation: embeddings vs "active" params, and how model size couples to data budget](20260711_parameter-allocation.md)
